<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{titulo}}</title>
</head>
<body>
    {{> header}}
    <h1>{{titulo}}</h1>
    <ul>
        {{#each listaUsuarios}}
            <li>{{id}} - {{nombre}}</li>
        {{/each}}
    </ul>
    {{> footer}}
</body>
</html>